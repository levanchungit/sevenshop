import React from 'react';
import { Text, View } from 'react-native';

type Props = object;

const Home = (props: Props) => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
