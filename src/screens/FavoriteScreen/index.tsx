import { Text, View } from 'native-base';

const FavoriteScreen = () => {
  return (
    <View style={{ flex: 1, paddingTop: 12, paddingHorizontal: 10 }}>
      <Text style={{ fontSize: 18 }}>Favorite Screen</Text>
    </View>
  );
};

export default FavoriteScreen;
