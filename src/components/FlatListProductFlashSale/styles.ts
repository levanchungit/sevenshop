import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  flastListFlashSale: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
});
export default styles;
